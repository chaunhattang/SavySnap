import UnifiedAuthContainer from '@/components/auth/register/UnifiedAuthContainer';

const RegisterPage = () => {
    return <UnifiedAuthContainer defaultView="register" />;
};

export default RegisterPage;
